@model Gerenciador_de_Consultas_Médicas.Models.consultas
@*@model IEnumerable<Gerenciador_de_Consultas_Médicas.Models.consultas>*@

@{
    ViewBag.Title = "Create";
}

<h2>Marcar consulta</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>consultas</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        <div class="form-group">
            @Html.Label("Especialidades", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("especialidades_idEspecialidade", null, new { onchange = "this.form.submit();" })
            </div>
        </div>
       
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Pesquisar" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
